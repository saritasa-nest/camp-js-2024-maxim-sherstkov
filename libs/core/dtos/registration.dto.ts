/** Registration data DTO. */
export type RegistrationDto = Readonly<{

	/** Email. */
	email: string;

	/** Password. */
	password: string;

	/** First name. */
	first_name: string;

	/** Last name. */
	last_name: string;
}>;
